{
  "pluginAlias": "qBittorrentHomebridgePlugin",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Exposes one HomeKit switch per qBittorrent server, toggling that server's **alternative speed limits**.\n\nEnable the Web UI on every server you add (*Tools \u2192 Options \u2192 Web UI*).",
  "footerDisplay": "A switch is tied to its server, so renaming a server or changing its address keeps its HomeKit room, scenes and automations.\n\nHaving trouble? See the [README](https://github.com/RauchenwaldC/homebridge-qbittorrent-plugin?tab=readme-ov-file#qbittorrent-homebridge-plugin) or open an [issue](https://github.com/RauchenwaldC/homebridge-qbittorrent-plugin/issues).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "description": "Shown in the Homebridge log. Not the name of any switch.",
        "type": "string",
        "required": true,
        "default": "qBittorrent"
      },
      "servers": {
        "title": "qBittorrent Servers",
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "title": "qBittorrent Server",
          "properties": {
            "name": {
              "title": "Name",
              "description": "The switch name in the Home app. Give each server a different name.",
              "type": "string",
              "required": true,
              "minLength": 1,
              "maxLength": 64,
              "placeholder": "qBittorrent"
            },
            "apiUrl": {
              "title": "Web UI URL",
              "description": "Address of the qBittorrent Web UI, including the port.",
              "type": "string",
              "required": true,
              "minLength": 1,
              "placeholder": "http://localhost:8080"
            },
            "username": {
              "title": "Username",
              "description": "Leave empty if this server does not require authentication.",
              "type": "string"
            },
            "password": {
              "title": "Password",
              "description": "Leave empty if this server does not require authentication.",
              "type": "string",
              "x-schema-form": {
                "type": "password"
              }
            },
            "refreshInterval": {
              "title": "Refresh Interval (seconds)",
              "description": "Overrides the global default for this server only. Clear this field to go back to using the global value. Between 5 and 3600.",
              "type": "integer"
            },
            "requestTimeout": {
              "title": "Request Timeout (seconds)",
              "description": "Overrides the global default for this server only. Clear this field to go back to using the global value. Between 1 and 60.",
              "type": "integer"
            }
          }
        }
      },
      "refreshInterval": {
        "title": "Refresh Interval (seconds)",
        "description": "Applies to every server, unless that server overrides it in its own Advanced section.",
        "type": "integer",
        "minimum": 5,
        "maximum": 3600,
        "default": 30
      },
      "requestTimeout": {
        "title": "Request Timeout (seconds)",
        "description": "Applies to every server, unless that server overrides it in its own Advanced section.",
        "type": "integer",
        "minimum": 1,
        "maximum": 60,
        "default": 10
      }
    }
  },
  "layout": [
    {
      "key": "servers",
      "type": "array",
      "notitle": true,
      "orderable": false,
      "buttonText": "Add Server",
      "items": [
        {
          "type": "fieldset",
          "expandable": true,
          "expanded": true,
          "title": "{{ value.name || 'Server #' + $index }}",
          "items": [
            "servers[].name",
            "servers[].apiUrl",
            "servers[].username",
            "servers[].password",
            {
              "type": "fieldset",
              "title": "Advanced \u2014 overrides the global defaults",
              "expandable": true,
              "expanded": false,
              "items": [
                "servers[].refreshInterval",
                "servers[].requestTimeout"
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "fieldset",
      "title": "Global Defaults For All Servers",
      "expandable": true,
      "expanded": false,
      "items": [
        "refreshInterval",
        "requestTimeout"
      ]
    }
  ]
}
